machine:
  node:
    version: 4
test:
  override:
    - yarn coverage
deployment:
  staging:
    branch: master
    commands:
      - yarn deploy staging
  production:
    tag: /^[0-9]+(\.[0-9]+)*/
    commands:
      - yarn deploy production
general:
  artifacts:
    - "test/coverage"
